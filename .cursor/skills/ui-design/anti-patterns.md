# Anti-Patterns (Auto-Updates)

Append new entries from feedback. Format: `- Ban: [issue] ([source/file])`.

- Ban: Generic purple gradients and generic AI-style visuals (init)
- Ban: Perfect symmetry; add 5–15% offsets for interest (init)
- Ban: Uniform spacing everywhere; vary spacing (e.g. 4–24px) (init)
- Ban: Primary actions (e.g. Save) only at top of long scroll; use sticky header or sticky bottom bar so actions stay reachable on mobile (2026-03-08-save-button-mobile)
- Ban: Context/dropdown menus that open outside the viewport; use portal + fixed positioning with viewport clamping so menu stays on-screen (IdeaCard menu)
