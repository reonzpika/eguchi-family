# Design System (Evolves)

Extend from feedback. Prefer project `docs/design-system.md` when present for full tokens and components.

## Tokens (defaults; override from project)

```yaml
colors:
  primary: "#0070f3"
  accent: "#ff6b6b"
spacing:
  xs: "4px"
  s: "8px"
  m: "16px"
  l: "24px"
```

## Components

- **button**: min-height 44px (touch target), clear default/hover/focus states.
